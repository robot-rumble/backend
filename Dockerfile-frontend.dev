FROM node:latest
WORKDIR /frontend

COPY src/frontend/package.json ./
RUN npm install

COPY src/frontend ./

# needed for elm to install packages
RUN node_modules/.bin/elm make src/*.elm

EXPOSE 8080
CMD DOCKER=true npm run watch

